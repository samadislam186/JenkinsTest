pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo "Running successfully"
            }
        }
		stage('Test'){
		steps {
			echo "running"
			}
		}
    }
}
pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo "Running successfully"
            }
        }
	stage('Deploy') {
		steps {
	        	echo "deployed"
		}
	}	
    }
}